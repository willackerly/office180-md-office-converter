interface:
  display_name: "PPTV Authoring"
  short_description: "Author atom-first no-reflow visual source"
  default_prompt: "Use $pptv-authoring to create, read, compare, edit, or compile a fully hydrated SVG atom, using HTML only for a real deck or report."
