interface:
  display_name: "PPTV Legacy Compatibility"
  short_description: "Inspect or migrate legacy PPTV source"
  default_prompt: "Use $pptv-authoring only to inspect legacy PPTV, migrate one legacy SVG atom, or extract one legacy deck slide, then follow the canonical $vector180-authoring workflow without silently rewriting source."
