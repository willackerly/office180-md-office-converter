name: CI

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.20.0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m venv .venv
          .venv/bin/python -m pip install --disable-pip-version-check --editable .
          pnpm install --frozen-lockfile

      - name: Test installed Python package
        run: |
          .venv/bin/python -m pip check
          .venv/bin/python tests/test_python_package.py

      - name: Rebar Tier 3 gates
        run: scripts/ci-check.sh --strict

      - name: Format
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Frozen PPTV compatibility build
        run: pnpm legacy:build

      - name: Package dry run
        run: pnpm pack:check

      - name: Dependency audit
        run: pnpm audit --audit-level high

  python-package-minimum:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up minimum Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: Install and test Python package
        run: |
          python -m venv .package-venv
          .package-venv/bin/python -m pip install --disable-pip-version-check --editable .
          .package-venv/bin/python -m pip check
          .package-venv/bin/python tests/test_python_package.py

      - name: Build Python wheel
        run: >-
          .package-venv/bin/python -m pip wheel
          --disable-pip-version-check
          --no-deps
          --wheel-dir "${RUNNER_TEMP}/office180-python-dist"
          .

  browser-chromium:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.20.0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Browser conformance and editor flow
        run: pnpm test:browser

  browser-release-matrix:
    if: github.event_name == 'pull_request' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        browser:
          - firefox
          - webkit

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.20.0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install ${{ matrix.browser }}
        run: pnpm exec playwright install --with-deps ${{ matrix.browser }}

      - name: Build browser artifacts
        run: pnpm browser:check

      - name: Browser conformance and editor flow
        run: >-
          pnpm --filter @office180/vector180 exec playwright test
          --project=${{ matrix.browser }}
